export type BlogsType = {
  body: string;
  id: number;
  title: string;
  user_id: number;
};

export type CommentType = {
  body: string;
  email: string;
  id: number;
  name: string;
  user_id: number;
};
